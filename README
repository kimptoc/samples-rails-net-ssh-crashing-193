This is a minimal Rails project using net-ssh in an initializer that crashes the 1.9.3-p0 rvm

See ssh_test.rb for the bit that triggers the crash

Just start the rails server and it will crash

See rails_crash.txt for the output that is produced when it crashes.